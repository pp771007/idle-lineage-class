# 上游差異分析 v3.4.83 → v3.5.49（2026-07-18）

- **BASE**（上次分析）：`f0c9a4ef12450ec86f1be7cb85cb21c106fa4a3e`（上游 v3.4.83）
- **TARGET**（本次最新）：`5481e2b4ebb2fe88abc7ad26fa4ab6d2972e2e16`（上游 v3.5.49）
- 18 個 commit；js 幾乎全檔異動、`css/style.css +306`、`index.html` 換版並新增 `js/24`。
- 美術資產：`classanim +14070`、`morphanim +10785/改6214/刪175`、`anim +2191/改59`、`sfx +36`、`icons +32/-1`、`area +4`、`bgm +2`。
- 上游 commit message 全是「1」，一律以 diff 為準。

## 一句話總結

這輪是**大型內容 + 系統重構雙主軸**：主軸內容是全新日式妖怪資料片「**日出之國**」（時空裂痕第三區，4 圖 / 13 一般怪 / 5 頭目含三段變身 / 16 遺物 / 滅魔防具 / 傳說），主軸系統是**「施法速度與攻擊速度徹底分家」的跨檔大重構**（延續上一輪的公式重寫，橫跨 01/02/03/06/07）＋**變身/立繪系統大改**（真夏納職業式變身、傭兵也能顯示變身立繪、三方向動畫、js/09 `_actorBattleForm` 重構）。另有一個**全新獨立子系統「潘朵拉遺物市場 + 流浪玩家收購」**（`js/24`，1102 行，跨角色共用、走 wall-clock、不影響離線結算）。

移植難度兩極：**新內容/新子系統多為低衝突、可整包挑**；**系統重構類全部落在我方已分岔的熱區（施法/變身/戰鬥掛點），機械合併必撞，屬閘門級決策**。

---

## 分類 A — 閘門級大整包（牽一髮動全身，先決策再動）

### A1. 施法速度與攻擊速度徹底分家（跨 01/02/03/06/07）🔴 最大整包
- **玩家視角**：以前放法術的快慢跟著「攻擊速度」走（喝加速藥、勇敢、精靈餅乾、攻速精通、變身都會讓法術也放更勤）。改版後**攻速與施法速度完全分離**——施法間隔只看「職業/變身的施法速度」，攻速加成不再加快施法；且「攻擊法術」與「輔助法術（治癒/淨化/轉換/團隊 HoT）」變成兩條各自獨立的速度；手動施法冷卻也從固定 1 秒改吃施法速度公式。
- **公式改前→改後**：舊 `getAutoCastInterval()=max(1,round(20*spdMult))`（吃攻速）→ 新委派 js/01 新函式 `castIntervalTicks(who,support)`/`nextCastCooldown(...)`，只讀職業/變身 cast。js/02 新增 `d.supportCastLock`；js/06 每名傭兵新增 `_healCastCd/_convertSkillCd/_purifySkillCd`；js/07 轉換魔法從 `autoActions`（每 30 tick）搬到 `autoCastSpells`（每 tick）。
- **順帶夾的攻速「名實相符」數值修正**（同包，js/02+03）：加速術/勇敢/裝備加速 `*0.67`→`*(1/1.33)`（舊 0.67 其實是 +49%，改後真的 +33%）；精靈餅乾/行走加速 `0.85`→`(1/1.15)`；血之渴望 `0.85`→`(1/1.15)`；切割 `0.80/0.50`→`(1/1.2)/(1/1.5)`；舊套裝變身 `pf.spd`。
- **assets**：無。
- **衝突風險：極高（整支需重寫）**。我方 js/01 沒有 `castIntervalTicks/nextCastCooldown`（地基缺失）；js/02 仍舊模型、無 `supportCastLock`、且我方在 spdMult 區塊自加了上游沒有的遺物攻速詞綴（`atkSpdPct`/`meleeHaste`/`polyAtkSpdPct`，**移植必須保留**）；js/03 攻擊/tick 段已分岔；js/07 轉換仍在 autoActions；js/06 傭兵仍舊 interval。要移植＝把 js/01 新函式搬進來後**逐檔重寫**已分岔的 cast 段。
- **移植狀態**：未移植（閘門級，需使用者決策是否要跟進這套施法模型）。

### A2. 攻擊間隔保留小數 tick（js/03）
- **玩家視角**：攻擊間隔（如 0.19 秒）以前被無條件捨去成 0.1 秒（偷偷變快），改版保留小數餘額，長期平均攻速才準；攻速中途變動時已累積進度按比例保留。
- **涉及**：新 `playerAttackIntervalTicks(includeTemporarySlow)`；`state.pDmgTick` 改帶小數餘額。
- **衝突風險：中**。我方 `pDmgTick++` 仍整數版且攻擊段已改（硬直 `-=hs`）。與 A1 同屬 js/03 攻擊段，**建議一起處理或一起不動**。
- **移植狀態**：未移植（與 A1 綁定）。

### A3. 移動速度統一換算 `playerMoveDelayMultiplier()`（js/02 + js/03 出怪重生）
- **玩家視角**：把變身 wlk/加速/勇敢/精靈餅乾/行走加速/神聖疾走/風之疾走/裝備移速統一成一個移速倍率，資訊面板顯示與怪物重生排程共用同一套。
- **衝突風險：高（撞我方刻意偏離）**。上游在 `tick()` 內就地簡化重生延遲段；**我方已把整塊抽成 `maybeSpawnMobs()`**（供離線快速結算共用），上游 hunk 不能直接吃，要套進我方 `maybeSpawnMobs()`。且它用到 A1 的 1/1.33 新數值與疾走互斥旗標。
- **移植狀態**：未移植（與 A1 連動）。

### A4. 變身系統大改（遠近武器分流 + APM 精算 + 夏納/真夏納，js/02）
- **玩家視角**：遠距武器只能用遠距變身、近戰/空手只能用近距變身，換武器立即重抽；變形控制戒指選單過濾不符武器（硬選被拒）。無戒指的隨機變身改從「自身等級以下全部相符類型」抽。多個變身攻速改 APM 精算（避免百分秒累積誤差）。新增 Lv80 遠距變身「莉絲安」（弓）。另有**變形控制戒指專屬**的「夏納」(Lv52，保留原職業外觀)與「真夏納」(Lv85，16 職業性別專屬變身動態，普通卷軸永遠抽不到)。
- **涉及**：`RANGED_POLY_FORMS`、`polyFormMatchesEquippedWeapon`、`polyRandomCandidates`、`POLY_TIERS` 加 `atkApm/castApm`、`SHANNA_FORM`/`TRUE_SHANNA_FORM`、`CONTROL_ONLY_POLY_FORMS`、`shannaSpeedForActor`/`trueShannaSpeedForActor`。
- **assets：是**（真夏納 `classMorph` 需 `assets/classanim/真夏納<avatar>`，見 B4；夏納/莉絲安不需新美術）。
- **衝突風險：中—高**。落在我方 js/02 已分岔的 spdMult/變身覆蓋區；且**依賴 A1 的 `supportCastLock` 與 APM 系統**，我方沒有這套，需連 A1 一起才裝得起來。
- **移植狀態**：未移植（與 A1 綁定）。

> **A1–A4 是同一個閘門**：施法/攻速/移速/變身四者在上游是一整包互相依賴的重構，落點全在我方大改過的戰鬥熱區。要跟就整包跟、要重寫我方分岔段；四者拆開單挑幾乎不可行。**這是本輪最大的產品決策點。**

### A5.（附帶提醒）月光爆裂魔法公式我方仍是舊版
- 上游 `procMoonburst`/`allyProcMoonburst` 早前已改「受完整魔法傷害公式影響」；**我方仍是舊 `roll(1,30)+2*en` 固定傷害**（上一輪那個「方向魔法/治癒公式重寫」大整包我方尚未吃這塊）。若決定跟進魔法公式統一才連帶處理；它與 B3 的 terrorVisage 行綁在同幾行。

---

## 分類 B — 全新內容 / 子系統（多為低衝突，可整包挑）

### B1. 🌅 日出之國新地區（時空裂痕第三區）— 資料層
- **玩家視角**：全新日式妖怪地區，4 圖（`sunrise_castle/east/west/north`）、13 一般妖怪（嗚釜、鎌鼬、轆轤首、河童、赤鬼、青鬼、鵺、天狗、阿修羅像…）＋5 頭目（玉藻/九尾/殺生石＝三段變身、牛鬼 Lv90、巨大骷髏 Lv99）。新材料「封印的妖怪之魂」（×100 鍊「巨大骷髏的妖魂」＝用了給 100 萬經驗）、傳說「陰陽師的扇子」「巨型骷髏之指環」、體力戒指，16 件日出之國遺物。
- **涉及**：js/00 `DB.mobs` 加 `sr_*`、`DB.maps` 加 4 圖、`DB.items` 加材料/武器/遺物；js/01 `MOB_DROPS` 追加 16 隻掉落表、`ITEM_WEIGHTS`、`MOB_PARTY_AOE_SKILLS` 加 8 個技能名。js/11 `MAP_CATEGORIES.rift`/`MAP_REGIONS.rift`、js/13 `SPECIAL_AREA_BG`/`AREA_1920`、js/15 `CARD_REGIONS.rift`、index.html 標題改「日出之國」。
- **assets：是**（怪物 anim ~2191 檔、4 張 area 圖、2 BGM、~36 sfx、遺物/材料 icon）。
- **衝突風險：低**（多為 push/新增 key）。小衝突點：js/01 `MOB_PARTY_AOE_SKILLS`、js/11 `MAP_*.rift`、js/15 `CARD_REGIONS.rift` 我方都加過提卡爾 `tikal_*`（相鄰行），把兩邊清單接起來即可，trivial。
- **移植狀態**：未移植（新內容，可挑；但頭目三段變身/新遺物特效見 B3，需連帶處理）。

### B2. 🌅 三段變身頭目系統（玉藻→九尾→殺生石，js/05）
- **玩家視角**：打頭目血量到門檻（或被一擊打到 0）不死，原地滿血變身下一階，有變身名條特效；只有打倒最終階（殺生石）才給經驗/金幣/掉落/卡片，中間階不掉任何獎勵、不掉卡。
- **涉及**：js/05 新 `doMobTransform(idx)`、`killMob` 頂端兩道攔截（`_justTransformedTick` 守衛 + `transformTo` 攔截 return）、`_TRANSFORM_STAGE_IDS`（`pickRiftMob` 排除防跳關白拿）、`renderAuditDrops` 併鏈根。js/15 卡片變身鏈（`CARD_CHAIN_BY_FINAL`、中間階不掉卡）。頭目資料帶 `transformTo/transformHpPct/transformFxText/noHpCurve`。
- **衝突風險：中**。`killMob` 頂端撞我方 `__afkKillTally` 離線計殺行。**併入順序**：冪等守衛 → `_justTransformedTick` 守衛 → `transformTo` 攔截 return → `__afkKillTally`（計殺放變身攔截之後，中途變身才不誤計）。
- **移植狀態**：未移植（B1 的一部分）。

### B3. 🌅 日出之國戰鬥內容包（新異常狀態 / 新遺物特效 / 恐怖面貌，js/03/04/06）
- **玩家視角**：4 種新異常狀態（弱化 傷-5命-2 / 疾病 AC+8命-4 / 目盲 命-6 / 藥水霜化 治癒藥-50%）；巨大骷髏「恐怖的面貌」隨機免疫近/遠/魔傷害之一數秒；一票新遺物特效（毒鵺的黑尾 命中附毒、牛鬼系 `onHitPoison`、牛鬼之子的黑戒 中招回HP、九尾妖狐的怒火 武器第二觸發槽、水靈的魔力珠 水魔法附冰凍、黝黑的烈火皮囊 火牢傷害加倍、牛鬼的斷角 重擊固傷、鐮鼬的尾刃 居合必爆、被敲爛的半邊頭盔 免疫沉默、黑騎士精銳長槍+漆黑塔盾成對 格檔100%）。
- **涉及**：新 helper `terrorVisageOnDamage(mob,dmg,kind)` 灑進**幾乎每個直擊掛點**（js/03 共鳴光箭/月光爆裂/連射/雙擊/副手/奇古；js/04 爆破/spellProc/免費法術/冰裂/稻草人/魔爆；js/06 傭兵各同名路徑）；4 異常狀態分支散在 js/03/04/06/07；`vfxMiss()`（未命中飄字，cosmetic，`typeof` 守衛）。
- **衝突風險：高（大量交錯，我方刻意未移植）**。`terrorVisageOnDamage` 與各分支被插進我方也改過的行（`procMoonburst`/`allyAttackOnce`/`_allyDamageMob`/`enemyPhysicalAttack` 都已分岔），機械 merge 逐行撞。我方 js/04 已有一行註解明講「不連帶搬 terrorVisage」。
- **移植狀態**：未移植。**要玩 B1 的頭目/遺物就得吃 B3**，屬「把整個日出之國戰鬥包當獨立專案搬」，工作量大。

### B4. 全新子系統：潘朵拉遺物市場 + 流浪玩家收購（js/24，全新 1102 行）
- **玩家視角**：黑市面板底下多一塊「遺物布告欄」，安全區地圖不定時出現「流浪的其他玩家 NPC」在喊話收東西。兩者共用新貨幣「**龍之鑽石**」，**整組遊戲所有角色共用**（獨立 localStorage，不進角色存檔）。
  - **A. 收購賺鑽石**：每 10 分鐘 5% 機率在合格安全區生一名玩家 NPC（壽命 2h），頭像/立繪走 `assets/classanim/<職業>F/unarmed_idle_*`；它想收某件裝備，走過去點它交付（真的消耗背包/倉庫該裝備）換龍之鑽石。每 3 分鐘系統日誌廣播一次（**有判 `state.ff` → 補跑靜音**）；名字可點，選「吵死了」停廣播、「馬上到」強制傳送過去。
  - **B. 遺物布告欄花鑽石換遺物**：黑市輸入「遺物」→ 三類別按鈕，每次搜尋花 100 鑽石產一張委託（隨機一件你還沒有的該類遺物 + 三種需求物）；湊齊三需求兌換，該欄 24h 冷卻。
- **涉及**：`STORE_KEY='fb5_pandora_relic_market_v1'`、`_withStateLock` 跨分頁鎖、`WANDERER_CHANCE/CHECK_MS/RELIC_SEARCH_COST=100/BOARD_COOLDOWN_MS`；對外匯出 20+ 函式。index.html 已加 `<script src="js/24-...">`；css 加 `.pandora-*`/`.wandering-*`。
- **與離線結算互動：無影響（放心）**。全走 wall-clock `Date.now()`（非 `state.ticks`），沒掛 autoActions/每-tick 迴圈，靠三個真實計時器跑 → 離線補跑不重播、不拖慢；回遊戲時自己用時間差補。唯一接觸：`hurryToWanderingBuyer` 是新的 `changeMap(true)` 呼叫點（目標安全區，風險低，併版後跑一次確認我方 afk 掛點不誤判）。
- **與小百科/掉落查詢外掛相依：低**。
- **衝突風險：低**（全新檔 + CSS 乾淨新增）。但它與 B5（js/14 黑市改）綁在一起才完整。
- **移植狀態**：未移植（可整包挑，是本輪最完整的獨立新玩法）。

### B5. 潘朵拉黑市面板大改 + 玩家收購單 + 滅魔客製製作（js/14）
- **玩家視角**：黑市從 20 件改 24 件（3×8），輪換由每 5 分鐘改每 10 分鐘 1 件、在架 240 分鐘；面板頂端新增「收 ___」玩家收購單（喊價 ≤ 市場價就命中上架，卡片標「收購」tag）。輸入「遺物」整合進 B4 布告欄。新增「**滅魔裝備客製製作**」（宙斯之熔岩高崙 NPC，消耗 +7 以上「抗魔法鏈甲」+ 古老盔甲 + 1000 萬金，成品恆 +0）。移除舊抽獎機死碼。
- **涉及**：`PANDORA_SLOT_COUNT 20→24`、`PANDORA_SLOT_TICKS 3000→6000`、`PANDORA_LIFETIME_TICKS`；`pandoraSetBuyOrder`/`buyOrder` 存 `player.pandoraMarket2`；`SLAYER_RECIPES`/`buildSlayerCraftHTML`/`doSlayerCraft`；刪 `doPandoraGacha`/`doPandoraGacha10` 等。js/12 `WH_NO_STORE` 移除 `new_item_239`。
- **衝突風險：高**。我方在黑市區塊也大量客製（自訂 tooltip `_pandoraTipEl`、`renderSyslogPandora` 用 `lastIdx`、`getWeightedGachaResult` 改寫、CRAFT_RECIPES 加 `npc_atelier`/`npc_mystic_mage`、拿掉席琳製作按鈕、自訂掉落來源彙整）。上游把 `_pandoraStock`/`refreshPandoraMarket`/`pandoraRenderMarket`/`renderSyslogPandora`/`pandoraTipShow` 全改一輪，與我方高度重疊，需逐段手動 merge。另：**我方還留著上游這次刪掉的舊抽獎機死碼**（`doPandoraGacha` 等，我方檔 1428 行起），合併時一併移除對齊。
- **移植狀態**：未移植（B4 要完整就得配 B5，但這是本輪合併最痛的一支）。

### B5-Wiki. 潘朵拉小百科補充（若移植 B4/B5 才需要）
- 若移植遺物市場/收購/滅魔製作，需照 `/update-wiki`：滅魔 4 件走客製製作要補進 `afk-dex.js buildCraftIndex` + `afk-wiki.js renderCraft`；龍之鑽石取得方式、遺物布告欄委託是新「取得途徑」；日出之國遺物的取得。

### B6. 真夏納職業立繪 + 傭兵變身通用化 + 三方向動畫（js/09 `_actorBattleForm` 重構）
- **玩家視角**：以前只有主玩家能顯示變身立繪、傭兵一律走職業動畫；新版**傭兵也能顯示變身立繪**（含武器特效層）。真夏納變身依職業性別切 16 種專屬立繪、依武器切動作、依目標轉三方向。大量既有變身升級為三方向（右前/前/左前）。
- **涉及**：`_playerBattleForm`→`_actorBattleForm(actor,allyGrp)`、`_playerMorphName`→`_actorMorphName`、`TRUE_SHANNA_ANIM_SETS`、`MORPH_ANIM_3DIR`（79 形態）、`_partySpriteXs` 傭兵站位 3→7 格、移除死碼 `_vfxImpact/_mobScatter`。js/19 `MORPH_ANIM_PORTRAIT` 加莉絲安、`_startMorphPortrait` 加 `tree` 參數走 classanim。
- **assets：是（超大宗）**。`classanim` 14070 檔（真夏納 16 職業×3 方向 = 48 資料夾）、`morphanim` ~1.7 萬檔異動（79 形態各補 F/2 三方向 + 真死亡騎士/莉絲安/炎魔/惡魔）。
- **衝突風險：高（3 處務必人工核對）**：
  1. `_partySpriteXs`：上游把 A 位擴 7 格硬編；**我方另解**用 `_allyXPct(i,len,…)` 等分佈 → 建議保留我方、丟上游硬編。
  2. `_playerBattleForm`/`_playerMorphName` 被改名重構，而我方在 `_playerMorphApply`/`_allySpritesApply` 內埋了 `_PM_ATTACK_SKILLS`/`_pmAbsorbSelfHpCost`（自傷技能受擊誤判修正）→ 套上游重構後**必須把這兩掛點植回新管線**。
  3. `state.ff && !state.ffSmall` sprite 守衛：我方已加多處；上游把同模式加進 `vfxBossEntrance/vfxBossRage`（向我方靠攏），但我方 `vfxBossEntrance` 用自訂 `_vfxOff()`，合併統一即可。
- **移植狀態**：未移植（美術量最大 + js/09 重構最痛；真夏納立繪也是 A4 的視覺前提）。

---

## 分類 C — 可獨立挑的中小功能（多為低衝突）

### C1. 金幣掉落公式整組重寫（js/05）
- **玩家視角**：一般怪金幣改吃統一曲線 `M=20+3L+0.06L²`，×[0.65,1.35] 再 ±10%；hard 非頭目 ×1.25；掉率一般怪 70%/頭目 100%；新增 `noGold` 旗標。
- **涉及**：新 `monsterGoldRange(mob)`，`killMob` 金幣段用 `_goldDropRate` 取代舊 `mob.goldMin||(lv*5)`。
- **衝突風險：低**（我方沒改金幣段）。但要注意與我方席琳 spawn 端 `goldMin×5/10` 的互動——曲線對一般怪重算會**忽略** spawn 乘算（頭目走 cfgMin 分支正常），移植時確認可接受。
- **移植狀態**：未移植（可獨立挑）。

### C2. 擊殺回血 killHealHp（食屍鬼的啃食面容頭盔，js/05）
- 戴該頭盔擊殺回 30 HP（玩家/傭兵各看自己頭盔，比照吉爾塔斯之劍掛點）。物品 `relic_ghoul_visage` + 掉落（深淵食屍鬼 0.0001%）。
- **衝突風險：中**（落在我方重寫過的 killMob 段，但可精準插在吉爾塔斯之劍 allies 行後、魔杖區塊前，定點插入）。**移植狀態**：未移植。

### C3. 武器分類補標 + 出血規則改寫（js/10）
- 修正一批天堂槍類武器類型誤判；出血改「單手矛出血、雙手矛只穿透不出血」（`weaponHasBleed()` 改 `矛 && !w2h`）。`WEAPON_TAGS` 大量補標。
- **衝突風險：高**。我方 `weaponHasBleed()` 是舊 `匕首||矛`（同行不同內容，直接衝突），`WEAPON_TAGS` 我方已大幅擴充（需逐條併，不可整段覆蓋）。**移植狀態**：未移植。

### C4. 疾走系技能改「移動速度」（js/00）
- `sk_holy_dash` 神聖疾走「迴避+15」→ 移速×1.33；`sk_elf_winddash` 風之疾走「迴避+10」→ 移速×1.33 且取代精靈餅乾（不疊加）。兩者互斥（js/10 `onAutoBuffToggle` 勾一個取消另一個）。
- **衝突風險：低**（我方兩條技能未改）。但 `moveSpeedMult` 實作端在 A3，**單帶資料改動無效，需 A3 一起**。**移植狀態**：未移植（與 A3 連動）。

### C5. 批量使用道具 + 經驗妖魂（js/08）
- 新道具「巨大骷髏的妖魂」使用得經驗；持多個時物品視窗多「批量使用（輸數量）」按鈕。`batchUseItem(u)`、`useItem` 加 `d.eff==='expsoul'` 分支。
- **衝突風險：中低**（`useItem` 是我方三參數改過的函式；expsoul 分支位置不打架，保留我方簽章嵌入即可）。**移植狀態**：未移植。

### C6. 全隊光環「隊友提供」圖示（js/08 + css）
- 隊友維持的全隊 buff 即使玩家沒開也在狀態列亮圖示（藍框+藍點區別自持）。`renderStatusIconBar` 掃 `TEAM_AURA_SKILLS`+`_teamAuraHas`（依賴 js/06），css `.status-icon--ally`。
- **衝突風險：低**（我方此段未改；需 js/06 有 `TEAM_AURA_SKILLS`，缺了 `typeof` 守衛只是不顯示）。**移植狀態**：未移植。

### C7. 武器/遺物 tooltip 用途摘要重構（js/10 `weaponPurposeLabels`）
- 背包/裝備/圖鑑/黑市武器說明，把「戰鬥實際生效但特效列沒列」的機制集中補齊；攻速 tooltip 由「施法冷卻下限」改「施法速度每分鐘」。
- **衝突風險：高**。我方 js/10 tooltip 段已顯著改寫（結構不同、仍用舊 `castLockTicks`），整塊要以我方為主逐項比對，不能套上游整段。**移植狀態**：未移植（且攻速 tooltip 依賴 A1）。

### C8. 攻速/移速顯示改用實際值（js/08 `_updateUIImpl`）
- 攻速改顯示 `playerAttackIntervalTicks`、移速改 `playerEffectiveMoveSpeedPct`（帶 `typeof` 守衛回退）。
- **衝突風險：低**（依賴 A2/A3 的新函式，缺了走回退值）。**移植狀態**：未移植（與 A2/A3 連動）。

### C9. 巨大骷髏「反傷之壁」寵物/召喚掛點（js/22/23）
- 巨大骷髏依近/遠/魔豎不同反傷壁；寵物普攻算近、寵物技能算魔、召喚普攻算近、召喚 AoE 算魔。`terrorVisageOnDamage` 呼叫點插進 js/22/23（`typeof` 守衛）。
- **衝突風險：中**（插入位置正是我方 pets/summons 大改的 `curHp-=dmg` 行，需逐行核對）。**移植狀態**：未移植（B3 的一部分）。

### C10. 一批 clean 小修正（js/00/01/05）
- `wpn_17` 補穿透 35%；`wpn_damascus` 反擊改 WEAPON_TAGS 賦予 + `ignHardSkin`；`wpn_giltas_wand` 說明「魔法點+10」→「+20」；移除 `new_item_239` 潘朵拉抽獎卷；`pirate_wild` 移除 `mambo_rabbit`；**滅魔系列防具 4 件** `amr_slayer_*`（`mrPerEn` 每強化+1 額外魔防）；遺物第十八批 5 件 + 0.0001% 掉落 + 負重；存檔壓縮 Worker raw-value 防呆；**AREA_BONUS 掉落率調整**（眠龍/妖精森林的粗糙米索莉塊/元素石 20%→2%、精靈玉維持 20%）；`atkSpdFamily` 修 isBow 誤判（名稱以「箭」結尾但實為弓的隱藏怪武器）；裂痕頭目補播出場特效。
- **衝突風險：多為 low/clean**（`wpn_giltas_wand` 我方自帶 +10 版，改字即可；其餘我方未觸及）。**移植狀態**：未移植（可挑）。

### C11. 音效/BGM/名條配色（js/17/09）
- 17 怪新增 attack/hurt/kill 音效表；HUNT_BGM 加日出之國 4 圖；`BOSS_ENTRANCE_FX` 加 5 頭目名條配色 + `boss:true` 屬性配色後備。
- **衝突風險：低**（純新增資料表）。**移植狀態**：未移植（B1 的音效部分）。

### C12. 傭兵變身自動維持（js/06 `allyMaintainPoly`）
- 傭兵變身自動維持、到期自動買變形卷軸；`_migrateMercPoly`/`buildAlly` 保留來源存檔變身。
- **衝突風險：中**（落在我方已分岔的 `allyMaintainBuffs`/`buildAlly` 區）。**移植狀態**：未移植。

---

## 分類 D — 我方刻意偏離上游對照（以我方為準，勿無腦帶入）

| 我方偏離 | 檔 | 本輪上游是否碰到 | 處置 |
|---|---|---|---|
| `__afkKillTally` 離線計殺 | js/05 killMob 頂端 | 上游插變身守衛同位置 | 保留；計殺放變身攔截**之後**（見 B2） |
| 殺王 `saveGame` `!state.ff` 守衛 | js/05 | 未動 | 保留 |
| 王族 `royalAllyMult(){return 1}` + 傭兵上限 7 | js/06 | 未改本體（只被加 terrorVisage 那行） | 保留；移植 B3 行時勿帶入上游王族×8 |
| `_isMercSelfBuff`/`allyMaintainBuffs` config-gating | js/06 | 加了一行「疾走純移速傭兵不維持」+ `allyMaintainPoly` | 併意圖進我方版（小重疊，見 C12） |
| `maybeSpawnMobs` 自 tick 抽出 | js/03 | 上游在 tick inline 簡化重生延遲 | 套進我方 `maybeSpawnMobs()`（見 A3） |
| `_PM_ATTACK_SKILLS`/`_pmAbsorbSelfHpCost` | js/09 | 所在函式被改名重構 | 植回新 `_actorBattleForm` 管線（見 B6） |
| `_allyXPct` 傭兵等分佈站位 | js/09 | 上游硬編 7 格 | 保留我方、丟上游硬編（見 B6） |
| sprite ticker `state.ff && !state.ffSmall` 守衛 | js/09 | 上游把同模式加進 vfxBoss* | 對齊即可 |
| `equipItem(item,silent)`/`useItem(u,silent,keepModal)` | js/08 | useItem 仍兩參數，插 expsoul/藥水加項 | 保留三參數簽章，只嵌上游新分支 |
| `saveOnExit` 去重 / `_trimLog` 錨定 | js/13 | 未動 | 保留 |
| 離線掛點 `offlineStamp/PreLoad/AfterLoad` | js/11/13 | 未動（loadGame 只加一行 `convertSk` migration） | 保留掛點，補該行別動掛點 |
| `startGame` 多職業起始武器 | js/13 | else 分支 `wpn_dagger1`→`wpn_11` | 我方多分支結構為準，1 行改動另評估 |
| `crushDr` 經典模式獨立顯示 | js/04/10 | 同區插 `_lancePair` 格檔（相鄰不同功能） | 保留；小重疊 |

---

## 移植成本速覽

- **美術資產（若移植 B1/B6）**：classanim 14070 + morphanim ~1.7 萬異動 + anim ~2191 + area 4 + bgm 2 + sfx 36 + icons ~32。搬檔一律 `git archive <上游> HEAD "<路徑>" | tar -x -C .`（中文檔名安全），完成後**必跑 `node scripts/gen-manifests.mjs` + gen-anim-manifest 重生**（否則 PWA 卡舊圖/離線 404、動畫少載幀）。這台一次推 >3MB 會斷線 → 照既有「切 ~2MB 線性 commit 分批推」（見專案 memory）。
- **程式合併難點集中**：js/09 `_actorBattleForm` 重構 × 我方掛點/站位（B6 三處）、js/14 黑市（B5）、施法重構整包（A1–A4）。
- **anim-manifest.js**：我方自產，移植資產後重生，勿手改/diff 合併。

## 建議的挑選視角（供使用者參考，非決定）

- **最完整、最低衝突的獨立新玩法** → B4+B5（潘朵拉遺物市場/收購/滅魔製作），但 B5 合併痛。
- **最大新內容** → B1（日出之國）；但要能真正打頭目/用遺物就得吃 B2+B3（戰鬥包，工作量大）。
- **視覺升級** → B6（真夏納 + 三方向 + 傭兵變身），美術量最大、js/09 重構最痛。
- **系統重構整包** → A1–A4，閘門級產品決策，牽動整個戰鬥手感，先想清楚要不要改變我方現有平衡再動。
- **好挑的糖** → C1（金幣公式）、C6（全隊光環圖示）、C10（小修正合集）、C5（批量使用）、C11（音效）。

---

> 本報告為 A 階段（差異分析）產物，**尚未挑選任何項目移植**。移植請照 `/upstream-diff` B 階段逐功能獨立 commit、3-way `git apply -3`，並過收尾檢查（smoke / update-wiki / 離線掛機評估 / gen-manifests）。
